{
  "name": "catslimdown-root",
  "version": "1.0.0",
  "description": "Root-level package configuration for CatSlimDown to run frontend and backend concurrently.",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "deploy:build": "node deploy-build.js",
    "test": "npm run test --prefix backend",
    "test:coverage": "npm run test:coverage --prefix backend",
    "test:e2e": "npx cypress run --project ./frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "sharp": "^0.35.3"
  }
}
